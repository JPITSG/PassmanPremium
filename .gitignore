# Build artifacts — produced by ./build.sh (see README.md)
passman-premium.xpi
passman-premium-unsigned.xpi

# AMO listing assets (generated from icons/icon.svg)
assets/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Tooling / dependency directories (if introduced later)
node_modules/

# Signing workflow — credentials, signed artifacts, local runbook
.amo-credentials
web-ext-artifacts/
AGENTS.md
